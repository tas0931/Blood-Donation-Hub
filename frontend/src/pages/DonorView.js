import React from "react";
import { Container, Typography } from "@mui/material";

const DonorView = () => {
  return (
    <Container>
      <Typography variant="h4">Donor Dashboard</Typography>
      <p>Coming soon: Blood donation history, requests, and more!</p>
    </Container>
  );
};

export default DonorView;
